# V1 - Azure/Databricks

Esta pasta preserva a versao original do projeto.

```text
v1/
  bronze/
  silver/
  gold/
  pipeline/
  config_adls.py
  data/
  docs/
```

Objetivo desta pasta:

- manter o historico do pipeline Azure/Databricks;
- servir como referencia para a refatoracao V2;
- evitar misturar codigo original com testes locais.

A V2 local fica em `../v2`.

